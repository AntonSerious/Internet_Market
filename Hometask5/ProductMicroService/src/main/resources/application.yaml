server:
  port: 8190
  servlet:
    context-path: /iMarket
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/imarket?serverTimezone=UTC
    username: root
    password: admin
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  application:
    name: ProductMicroService
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
    fetchRegistry: true
  instance:
    hostname: localhost
jwt:
  lifetime: 3600000
  secret: 3f9hwf9402-439)()U)0jjn0hgi^%&T())hohdh09y90--9